rm -rf *.bz2 *bundle
for file in `lynx --dump https://dist.whosonfirst.org/bundles/ | fgrep https:// | fgrep venue- | fgrep -v latest | awk '{ print $2; }'`; do
	# echo $file:
	wget --no-check-certificate $file
	tar jxf $file&
done
