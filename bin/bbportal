#!/usr/bin/env perl

use strict;
use warnings;
use autodie qw(:all);
use JSON::MaybeXS;

open(my $fin, '-|', 'python3 ./bbportal.py 2> /dev/null');

my @lines;

while(my $line = <$fin>) {
	next if($line =~ /Empty page on/);
	print $line;
	# chomp $line;
	push @lines, $line;
}
close $fin;
