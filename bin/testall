#!/usr/bin/env bash

set -e

cd ~/src
for i in $(find . -type d -name .git | sed s/\\/.git$//); do
        cd ~/src/$i
	if [ -f Makefile.PL ]; then
		git pull
		perlbrew exec 'perl Makefile.PL && make && make test AUTHOR_TESTING=1'
	fi
done
